# Code Logger CLI

CLI for Code Logger — use from any IDE or terminal. Requires the backend (and Qdrant) to be running.

Install: `pip install -e .` or `pip install .`  
Usage: `code-logger health`, `code-logger init`, `code-logger answer-query "your question"`
